Spring:
  datasource:
    url: "jdbc:mysql://localhost/board?useSSL=false "
    username: "root"
    password: "kyk12050713~"
    driver-class-name: com.mysql.cj.jdbc.Driver

ㅓpa:
  database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  open-in-view: false
  show-sql: true
  hibernate:
    format_sql: true
    ddl-auto: create

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
